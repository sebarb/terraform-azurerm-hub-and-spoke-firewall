#cloud-config

package_update: true
packages:
  - apache2

runcmd:
  - systemctl enable apache2
  - systemctl start apache2
  - echo "Hello from $(hostname -I | awk '{print $1}')" > /var/www/html/index.html